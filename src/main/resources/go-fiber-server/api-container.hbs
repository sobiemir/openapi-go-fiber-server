{{~#*inline "routing_entry"}}
	{{~#each operations.operation}}
	app.{{httpMethod}}("{{{basePathWithoutHost}}}{{{path}}}", {{varname}}.{{operationId}})
	{{~/each}}
{{~/inline~}}

package {{packageName}}

import "github.com/gofiber/fiber/v2"

func RegisterApiRouting(app *fiber.App) {
{{~#each apiInfo.apis}}
	{{operations.varname}} := {{operations.varname}}Controller{}
{{~/each}}
{{#each apiInfo.apis}}
	{{~>routing_entry varname=operations.varname}}
{{~/each}}
}
