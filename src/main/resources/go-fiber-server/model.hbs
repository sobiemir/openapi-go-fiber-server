{{!--
Handlebars template for models.

Default values are disabled because they are not working correctly.
What does not work:
	- $ref to enum and override example / description / default
	  quick fix - allOf.
	- $ref to enum and data inside the enum (default / example / descriptin) - should it work?
	  quick fix - allOf.
	- enum properties must be written this way: enum_prop or enumprop or ENUM_PROP or ENUMPROP
	  otherwise it will add _ for definition when there will be different cases.
Enum names should also have _, maybe add "underscore" function to handlebars engine?

The current solution is:


~--}}
package {{modelPackage}}
{{#each models}}
	{{~>misc/generic_imports bare=1 enl=1}}

	{{~#with model}}
		{{~>misc/nl n=1}}
		{{~>misc/description tab=0 enl=1}}

		{{~#if isEnum}}
			{{~>model/enum}}
		{{~else}}
			{{~>model/struct}}

func New{{classname}}() {{classname}} {
	return {{classname}} {
			{{~#each vars}}
		{{~>model/struct_default}}
			{{~/each}}
	}
}
		{{~/if}}
	{{~/with}}
{{~/each}}
